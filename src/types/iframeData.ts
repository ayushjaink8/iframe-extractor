export interface IframeData {
    id: string;
    srcdoc: string;
    path: string; // Hierarchical path (e.g., "0.1.2")
    displayName: string; // User-friendly name (e.g., "Iframe #1", "Iframe #2 (nested)")
  }
